---
phase: 2
plan: 03
title: "Command Integration and Wiring"
type: execute
wave: 2
depends_on: ["02-01"]
cross_phase_deps: ["01"]
autonomous: true
effort_override: thorough
skills_used: [code-review-pro]
files_modified:
  - cli/src/commands/apply.js
  - cli/src/commands/remove.js
forbidden_commands: []
must_haves:
  truths:
    - "apply.js imports applyPresetPlugins from ../lib/plugins.js"
    - "apply.js calls applyPresetPlugins when preset.plugins && preset.plugins.length > 0"
    - "apply.js passes preset.marketplaces ?? {} as the third argument to applyPresetPlugins"
    - "remove.js imports removePresetPlugins from ../lib/plugins.js"
    - "remove.js calls removePresetPlugins when snapshot.plugins && snapshot.plugins.length > 0"
    - "remove.js passes snapshot.marketplaces ?? {} as the third argument to removePresetPlugins"
    - "Plugin blocks are placed after the skills block in both files (consistent ordering)"
    - "No changes to state.js are required (snapshot stores full preset JSON automatically)"
  artifacts:
    - {path: "cli/src/commands/apply.js", provides: "Apply command with plugin support", contains: "applyPresetPlugins"}
    - {path: "cli/src/commands/remove.js", provides: "Remove command with plugin support", contains: "removePresetPlugins"}
  key_links:
    - {from: "cli/src/commands/apply.js", to: "cli/src/lib/plugins.js", via: "imports applyPresetPlugins"}
    - {from: "cli/src/commands/remove.js", to: "cli/src/lib/plugins.js", via: "imports removePresetPlugins"}
---
<objective>
Wire the plugin library into the apply and remove command flows. Add import statements and conditional blocks to `apply.js` and `remove.js` so that presets with a `plugins` array trigger `applyPresetPlugins`/`removePresetPlugins`. This completes the feature: presets can now declare plugins that are installed on apply and removed on remove.
</objective>
<context>
@.vbw-planning/phases/02-plugin-support/02-01-RESEARCH.md
  -- Research: exact insertion points in apply.js (after skills block, lines 66-68) and remove.js (after skills block, lines 36-38), state tracking is automatic
@.vbw-planning/codebase/CONVENTIONS.md
  -- Project naming and code style conventions
</context>
<tasks>
<task type="auto">
  <name>Wire applyPresetPlugins into apply.js</name>
  <files>
    cli/src/commands/apply.js
  </files>
  <action>
Modify `cli/src/commands/apply.js`:

1. Add import at the top with the other lib imports:
   ```js
   import { applyPresetPlugins } from '../lib/plugins.js';
   ```

2. Add a conditional block after the skills block (after the `if (preset.skills && preset.skills.length > 0)` block, before `addApplied`):
   ```js
   if (preset.plugins && preset.plugins.length > 0) {
     await applyPresetPlugins(workspaceRoot, preset.plugins, preset.marketplaces ?? {});
   }
   ```

3. Do not modify any other part of the file. The state tracking is automatic -- `addApplied(state, name, preset.version, preset)` stores the full preset JSON including the new `plugins` and `marketplaces` fields.
  </action>
  <verify>
`grep "applyPresetPlugins" cli/src/commands/apply.js` returns matches for both the import and the function call. `grep "preset.marketplaces" cli/src/commands/apply.js` confirms the marketplaces argument is passed.
  </verify>
  <done>
`cli/src/commands/apply.js` imports and calls `applyPresetPlugins` for presets with plugins.
  </done>
</task>
<task type="auto">
  <name>Wire removePresetPlugins into remove.js</name>
  <files>
    cli/src/commands/remove.js
  </files>
  <action>
Modify `cli/src/commands/remove.js`:

1. Add import at the top with the other lib imports:
   ```js
   import { removePresetPlugins } from '../lib/plugins.js';
   ```

2. Add a conditional block after the skills block (after the `if (snapshot.skills && snapshot.skills.length > 0)` block, before `await saveState`):
   ```js
   if (snapshot.plugins && snapshot.plugins.length > 0) {
     await removePresetPlugins(workspaceRoot, snapshot.plugins, snapshot.marketplaces ?? {});
   }
   ```

3. Do not modify any other part of the file. The error recovery pattern (re-pushing entry on failure) already covers plugin removal failures since it wraps the entire removal block.
  </action>
  <verify>
`grep "removePresetPlugins" cli/src/commands/remove.js` returns matches for both the import and the function call. `grep "snapshot.marketplaces" cli/src/commands/remove.js` confirms the marketplaces argument is passed.
  </verify>
  <done>
`cli/src/commands/remove.js` imports and calls `removePresetPlugins` for snapshots with plugins.
  </done>
</task>
<task type="auto">
  <name>End-to-end verification of plugin flow</name>
  <files>
    cli/src/commands/apply.js
    cli/src/commands/remove.js
    cli/src/lib/plugins.js
  </files>
  <action>
Verify the complete plugin flow works end-to-end by dry-running the apply and remove paths:

1. Confirm `cli/src/lib/plugins.js` is importable from the commands directory (correct relative path `../lib/plugins.js`).
2. Trace the apply flow: `apply.js` loads preset JSON (which now has `plugins` and `marketplaces`) -> checks `preset.plugins.length > 0` -> calls `applyPresetPlugins(workspaceRoot, plugins, marketplaces)` -> plugins.js reads settings, writes enabledPlugins, writes settings.
3. Trace the remove flow: `remove.js` gets snapshot from state (which has the stored `plugins` and `marketplaces`) -> checks `snapshot.plugins.length > 0` -> calls `removePresetPlugins(workspaceRoot, plugins, marketplaces)` -> plugins.js reads settings, deletes enabledPlugins keys, writes settings.
4. Run `node -e "import('./cli/src/lib/plugins.js').then(m => console.log(Object.keys(m)))"` from the project root to verify the module loads and exports the expected functions.
5. Verify no circular imports exist between plugins.js and settings.js.
  </action>
  <verify>
The module loads without errors and exports `applyPresetPlugins` and `removePresetPlugins`. The import paths in apply.js and remove.js are correct relative paths. No circular dependency warnings.
  </verify>
  <done>
End-to-end plugin flow verified: apply writes enabledPlugins, remove deletes them, state tracks the snapshot automatically.
  </done>
</task>
</tasks>
<verification>
1. `grep "import.*plugins" cli/src/commands/apply.js cli/src/commands/remove.js` -- both files import from plugins.js
2. `grep "applyPresetPlugins" cli/src/commands/apply.js` -- apply calls the function
3. `grep "removePresetPlugins" cli/src/commands/remove.js` -- remove calls the function
4. `grep "preset.marketplaces" cli/src/commands/apply.js` -- marketplaces passed on apply
5. `grep "snapshot.marketplaces" cli/src/commands/remove.js` -- marketplaces passed on remove
6. `node -e "import('./cli/src/lib/plugins.js').then(m => console.log(Object.keys(m)))"` -- exports both functions
7. `node -c cli/src/commands/apply.js && node -c cli/src/commands/remove.js` -- both files parse without syntax errors
</verification>
<success_criteria>
- apply.js imports and conditionally calls applyPresetPlugins after the skills block
- remove.js imports and conditionally calls removePresetPlugins after the skills block
- Both pass marketplaces as the third argument with ?? {} fallback
- No changes to state.js (snapshot stores full preset JSON automatically)
- Plugin blocks follow the same guard pattern as skills (check array exists and has length)
- The module loads and exports correctly (no import errors)
</success_criteria>
<output>
03-SUMMARY.md
</output>
