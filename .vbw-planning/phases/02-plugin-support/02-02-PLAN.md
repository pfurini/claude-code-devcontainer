---
phase: 2
plan: 02
title: "Preset JSON Updates"
type: execute
wave: 1
depends_on: []
cross_phase_deps: ["01"]
autonomous: true
effort_override: thorough
skills_used: [code-review-pro]
files_modified:
  - cli/presets/nextjs.json
  - cli/presets/convex.json
forbidden_commands: []
must_haves:
  truths:
    - "nextjs.json contains a plugins array with at least one entry for typescript-lsp@claude-plugins-official"
    - "nextjs.json plugin entries have name and marketplace fields; scope defaults to project"
    - "nextjs.json contains a marketplaces object (empty {} since only official marketplace is used)"
    - "convex.json contains plugins: [] (empty array) and marketplaces: {} since no Convex-specific plugin exists"
    - "Both preset JSON files have version bumped to 2.0.0 to reflect the schema change"
    - "Both preset JSON files remain valid JSON with consistent formatting (2-space indent)"
  artifacts:
    - {path: "cli/presets/nextjs.json", provides: "Next.js preset with plugin support", contains: "typescript-lsp"}
    - {path: "cli/presets/convex.json", provides: "Convex preset with plugin schema fields", contains: "plugins"}
  key_links:
    - {from: "cli/presets/nextjs.json", to: "cli/src/lib/plugins.js", via: "plugins array consumed by applyPresetPlugins"}
    - {from: "cli/presets/convex.json", to: "cli/src/lib/plugins.js", via: "plugins array consumed by applyPresetPlugins"}
---
<objective>
Update both v1 preset JSON files to include the new `plugins` and `marketplaces` fields. The Next.js preset gets the `typescript-lsp` plugin from the official marketplace. The Convex preset gets empty arrays (no Convex-specific plugin available). Both presets are version-bumped to 2.0.0 to signal the schema change.
</objective>
<context>
@.vbw-planning/phases/02-plugin-support/02-01-RESEARCH.md
  -- Research: proposed plugin schema, official marketplace plugins available, schema extension points
@.vbw-planning/codebase/CONVENTIONS.md
  -- Project naming and code style conventions
</context>
<tasks>
<task type="auto">
  <name>Add plugins field to nextjs.json</name>
  <files>
    cli/presets/nextjs.json
  </files>
  <action>
Update `cli/presets/nextjs.json`:

1. Bump `"version"` from `"1.0.0"` to `"2.0.0"`.
2. Add `"plugins"` array after `"skills"`, containing one entry:
   ```json
   {
     "name": "typescript-lsp",
     "marketplace": "claude-plugins-official",
     "scope": "project"
   }
   ```
3. Add `"marketplaces": {}` after `"plugins"` (empty because only the official marketplace is used, which is always available).
4. Preserve all existing fields (`settings`, `mcpServers`, `skills`, `claudeMd`) unchanged.
5. Maintain 2-space JSON indent formatting.

The `typescript-lsp` plugin provides TypeScript/JavaScript LSP integration, which is highly relevant for Next.js/TypeScript projects.
  </action>
  <verify>
`cat cli/presets/nextjs.json | python3 -m json.tool` validates JSON. `grep "typescript-lsp" cli/presets/nextjs.json` confirms the plugin entry. `grep '"version": "2.0.0"' cli/presets/nextjs.json` confirms version bump.
  </verify>
  <done>
`cli/presets/nextjs.json` has `plugins` array with typescript-lsp entry, `marketplaces: {}`, and version 2.0.0.
  </done>
</task>
<task type="auto">
  <name>Add plugins field to convex.json</name>
  <files>
    cli/presets/convex.json
  </files>
  <action>
Update `cli/presets/convex.json`:

1. Bump `"version"` from `"1.0.0"` to `"2.0.0"`.
2. Add `"plugins": []` after `"skills"` (empty -- no Convex-specific plugin in the official marketplace; Convex is served by its MCP server).
3. Add `"marketplaces": {}` after `"plugins"`.
4. Preserve all existing fields unchanged.
5. Maintain 2-space JSON indent formatting.

Adding the empty fields ensures schema consistency across all presets and signals that the preset format supports plugins even when none are configured.
  </action>
  <verify>
`cat cli/presets/convex.json | python3 -m json.tool` validates JSON. `grep '"plugins": \[\]' cli/presets/convex.json` confirms empty plugins. `grep '"version": "2.0.0"' cli/presets/convex.json` confirms version bump.
  </verify>
  <done>
`cli/presets/convex.json` has `plugins: []`, `marketplaces: {}`, and version 2.0.0.
  </done>
</task>
</tasks>
<verification>
1. `python3 -m json.tool cli/presets/nextjs.json > /dev/null` -- valid JSON
2. `python3 -m json.tool cli/presets/convex.json > /dev/null` -- valid JSON
3. `grep "typescript-lsp" cli/presets/nextjs.json` -- plugin present in nextjs
4. `grep '"plugins"' cli/presets/convex.json` -- plugins field present in convex
5. `grep '"2.0.0"' cli/presets/nextjs.json cli/presets/convex.json` -- both version-bumped
6. `grep '"marketplaces"' cli/presets/nextjs.json cli/presets/convex.json` -- both have marketplaces field
</verification>
<success_criteria>
- nextjs.json has a plugins array with typescript-lsp@claude-plugins-official entry
- convex.json has an empty plugins array (no Convex plugin available)
- Both presets have a marketplaces object (empty for v1)
- Both presets are version 2.0.0
- Both files are valid JSON with consistent formatting
- All existing fields (settings, mcpServers, skills, claudeMd) are preserved unchanged
</success_criteria>
<output>
02-SUMMARY.md
</output>
