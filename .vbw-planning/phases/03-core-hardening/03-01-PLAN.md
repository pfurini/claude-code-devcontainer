---
phase: 03
plan: 01
title: "Source Guard and Unit Tests"
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - install.sh
  - tests/test_install.sh
  - tests/fixtures/devcontainer_default_mounts.json
  - tests/fixtures/devcontainer_custom_mount.json
  - tests/fixtures/devcontainer_mixed_mounts.json
  - tests/fixtures/devcontainer_sys_admin.json
  - tests/fixtures/devcontainer_no_sys_admin.json
  - tests/fixtures/devcontainer_empty_runargs.json
  - tests/fixtures/devcontainer_malformed.json
forbidden_commands: []
must_haves:
  truths:
    - "install.sh can be sourced without executing main"
    - "All tests pass with exit code 0"
  artifacts:
    - {path: "install.sh", provides: "source guard", contains: "BASH_SOURCE"}
    - {path: "tests/test_install.sh", provides: "unit tests", contains: "check_no_sys_admin"}
    - {path: "tests/test_install.sh", provides: "unit tests", contains: "extract_mounts_to_file"}
    - {path: "tests/fixtures/devcontainer_sys_admin.json", provides: "test fixture", contains: "SYS_ADMIN"}
  key_links:
    - {from: "tests/test_install.sh", to: "install.sh", via: "source"}
---
<objective>
Add a source guard to install.sh so it can be sourced by test scripts without executing main, then create a comprehensive unit test suite covering the pure/testable functions: check_no_sys_admin, extract_mounts_to_file, merge_mounts_from_file, update_devcontainer_mounts, get_workspace_folder, and main arg parsing. REQ-05 (Security hardening).
</objective>
<context>
@.vbw-planning/phases/03-core-hardening/03-01-RESEARCH.md — Function purity classification, test surface analysis, source guard pattern, test script pattern, edge cases for each function, risk notes about set -euo pipefail when sourcing.
@.vbw-planning/phases/03-core-hardening/03-CONTEXT.md — Decisions: zero deps, raw shell assertions, source guard only refactoring, critical paths only coverage.
</context>
<tasks>
<task type="auto">
  <name>add-source-guard</name>
  <files>
    install.sh
  </files>
  <action>
Replace the unconditional `main "$@"` at the bottom of install.sh (line 864) with a source guard:
```bash
if [[ "${BASH_SOURCE[0]}" == "$0" ]]; then
  main "$@"
fi
```
This is the only change to install.sh. Do not restructure or refactor anything else.
  </action>
  <verify>
Run `bash -n install.sh` to confirm no syntax errors. Run `(source install.sh && type check_no_sys_admin)` in a subshell to confirm functions are available without main executing.
  </verify>
  <done>
install.sh contains `BASH_SOURCE` guard. Sourcing the file makes functions available without side effects.
  </done>
</task>
<task type="auto">
  <name>create-test-fixtures</name>
  <files>
    tests/fixtures/devcontainer_default_mounts.json
    tests/fixtures/devcontainer_custom_mount.json
    tests/fixtures/devcontainer_mixed_mounts.json
    tests/fixtures/devcontainer_sys_admin.json
    tests/fixtures/devcontainer_no_sys_admin.json
    tests/fixtures/devcontainer_empty_runargs.json
    tests/fixtures/devcontainer_malformed.json
  </files>
  <action>
Create `tests/fixtures/` directory with JSON fixture files for test cases:
- `devcontainer_sys_admin.json`: `{"runArgs": ["--cap-add=SYS_ADMIN"]}` — triggers check_no_sys_admin failure
- `devcontainer_no_sys_admin.json`: `{"runArgs": ["--cap-add=NET_ADMIN", "--cap-add=NET_RAW"]}` — passes check
- `devcontainer_empty_runargs.json`: `{"runArgs": []}` — passes check
- `devcontainer_malformed.json`: `{not valid json` — tests jq error handling
- `devcontainer_default_mounts.json`: mounts array with only the 8 default target paths (docker.sock, commandhistory, .claude, .config/gh, .gitconfig, .devcontainer, .agents, CLAUDE.md) — extract_mounts_to_file should return empty
- `devcontainer_custom_mount.json`: mounts array with one custom mount (e.g., `source=/data,target=/workspace/data,type=bind`) — extract_mounts_to_file should return it
- `devcontainer_mixed_mounts.json`: mounts array with default + custom mounts — extract_mounts_to_file should return only custom

Refer to research findings section 2 for the exact default target paths used in the filter.
  </action>
  <verify>
Run `jq . tests/fixtures/*.json` (skip malformed) to validate all JSON fixtures parse correctly. Confirm malformed fixture intentionally fails jq parsing.
  </verify>
  <done>
All 7 fixture files exist in tests/fixtures/ with valid test data.
  </done>
</task>
<task type="auto">
  <name>test-check-no-sys-admin</name>
  <files>
    tests/test_install.sh
  </files>
  <action>
Create `tests/test_install.sh` with the test harness (PASS/FAIL counters, assert helpers) and tests for `check_no_sys_admin`. The script must:
1. Start with `#!/bin/bash` and `set -euo pipefail`
2. Source install.sh using the source guard: `source "$(dirname "$0")/../install.sh"`
3. Define `PASS=0; FAIL=0` counters and assertion helpers (assert_exit for exit code checks, assert_eq for string equality)
4. Use subshells for exit-code assertions since `set -e` is active
5. Use `trap` to clean up temp dirs on EXIT
6. Test cases for check_no_sys_admin (use fixture files, create temp workspace dirs with .devcontainer/ structure):
   - No devcontainer.json -> exit 0
   - SYS_ADMIN in runArgs -> exit 1
   - NET_ADMIN only -> exit 0
   - Empty runArgs -> exit 0
   - Malformed JSON -> exit 0 (jq fails silently, function returns success)
7. Print summary at end: "Tests: $((PASS+FAIL)), Passed: $PASS, Failed: $FAIL"
8. Exit 1 if any FAIL > 0

Risk note from research: `set -euo pipefail` carries over from install.sh. Wrap failing-exit-code assertions in subshells to avoid premature script exit.
  </action>
  <verify>
Run `bash tests/test_install.sh` and confirm all check_no_sys_admin tests pass.
  </verify>
  <done>
tests/test_install.sh exists, is executable, and all check_no_sys_admin tests pass.
  </done>
</task>
<task type="auto">
  <name>test-mount-functions</name>
  <files>
    tests/test_install.sh
  </files>
  <action>
Add tests to tests/test_install.sh for the mount management functions:

**extract_mounts_to_file tests:**
- Missing file -> returns 0, empty output
- Default mounts only (use devcontainer_default_mounts.json fixture) -> returns empty string (no custom mounts)
- Custom mount present (use devcontainer_custom_mount.json) -> returns temp file path containing the custom mount
- Mixed mounts (use devcontainer_mixed_mounts.json) -> returns temp file with only custom mounts
- Clean up any temp files created by the function using trap

**update_devcontainer_mounts tests:**
- Add a new mount to a file with existing mounts -> mount appended
- Add a readonly mount -> output contains "readonly"
- Replace mount with same target -> only one mount with that target exists

**get_workspace_folder tests:**
- Returns basename of current directory when called without args

Note: merge_mounts_from_file wraps extract + update so testing the components is sufficient.
  </action>
  <verify>
Run `bash tests/test_install.sh` and confirm all mount function tests pass alongside the security tests.
  </verify>
  <done>
tests/test_install.sh contains passing tests for extract_mounts_to_file, update_devcontainer_mounts, and get_workspace_folder. Total test count >= 12.
  </done>
</task>
<task type="auto">
  <name>test-arg-parsing</name>
  <files>
    tests/test_install.sh
  </files>
  <action>
Add tests to tests/test_install.sh for main dispatcher arg parsing:

- No args -> exit 1 (prints usage)
- Unknown command (e.g., "foobar") -> exit 1
- Known commands that would call Docker: mock `docker` as a shell function that returns 0, then verify `main up .` does not error on the dispatch itself (it reaches the docker call). Override: `docker() { return 0; }` before calling main, then `unset -f docker` after.
- Verify `main` with valid subcommand dispatches without error (use a command like `help` or `version` if available, otherwise mock docker)

Wrap all exit-code assertions in subshells. Redirect stdout/stderr to /dev/null for clean output.
  </action>
  <verify>
Run `bash tests/test_install.sh` and confirm all tests pass. Verify total test count reported is >= 15.
  </verify>
  <done>
tests/test_install.sh passes all tests including arg parsing. Script exits 0 with summary showing 0 failures.
  </done>
</task>
</tasks>
<verification>
1. `bash -n install.sh` exits 0 (no syntax errors)
2. `(source install.sh && type check_no_sys_admin) 2>/dev/null` exits 0 (source guard works)
3. `bash tests/test_install.sh` exits 0 with 0 failures
4. `grep -c 'BASH_SOURCE' install.sh` returns >= 1
5. `ls tests/fixtures/*.json | wc -l` returns >= 7
</verification>
<success_criteria>
- install.sh has source guard allowing test scripts to source functions without side effects
- tests/test_install.sh passes with >= 15 test cases covering check_no_sys_admin, extract_mounts_to_file, update_devcontainer_mounts, get_workspace_folder, and arg parsing
- All test fixtures are valid JSON (except intentional malformed fixture)
- Zero external test dependencies (no bats-core, no npm)
</success_criteria>
<output>
03-01-SUMMARY.md
</output>
