---
phase: 1
plan: 02
title: "Preset Definitions (NextJS + Convex)"
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used: []
files_modified:
  - cli/presets/nextjs.json
  - cli/presets/convex.json
forbidden_commands: []
must_haves:
  truths:
    - "Both preset files are valid JSON conforming to the preset schema"
    - "nextjs.json includes next-devtools MCP server with correct npx args"
    - "convex.json includes convex MCP server with correct npx args"
    - "Both presets include claudeMd sections with framework-specific guidance"
    - "Permission allow arrays use correct Claude Code tool syntax (e.g., Bash(npm run *))"
    - "Preset names are lowercase slugs matching the filename stem"
  artifacts:
    - {path: "cli/presets/nextjs.json", provides: "Next.js App Router preset", contains: "next-devtools-mcp"}
    - {path: "cli/presets/convex.json", provides: "Convex BaaS preset", contains: "convex"}
  key_links:
    - {from: "cli/presets/nextjs.json", to: "cli/src/lib/settings.js", via: "settings field consumed by applyPresetSettings"}
    - {from: "cli/presets/convex.json", to: "cli/src/lib/mcp.js", via: "mcpServers field consumed by applyPresetMcp"}
---
<objective>
Create the two v1 preset definition files (NextJS and Convex) as JSON in `cli/presets/`. Each preset contains the full configuration surface: settings (permissions, env), MCP servers, skills references, and CLAUDE.md content. These are pure data files consumed by the apply/remove commands.
</objective>
<context>
@.vbw-planning/phases/01-preset-cli/01-01-RESEARCH.md
  -- Sections 2 (Claude Code config surface), 4 (preset JSON schema), R5 (concrete MCP entries for NextJS and Convex)
@.vbw-planning/phases/01-preset-cli/01-CONTEXT.md
  -- v1 presets: NextJS + Convex; full config surface per preset
</context>
<tasks>
<task type="auto">
  <name>Create NextJS preset definition</name>
  <files>
    cli/presets/nextjs.json
  </files>
  <action>
Create `cli/presets/nextjs.json` following the preset schema from Research section 4. Include:

**name**: `"nextjs"`
**version**: `"1.0.0"`
**description**: `"Next.js App Router preset for Claude Code"`

**settings**:
- `permissions.allow`: `["Bash(npm run *)", "Bash(npx *)"]` -- common Next.js dev commands
- `env`: `{ "NEXT_TELEMETRY_DISABLED": "1" }` -- disable Next.js telemetry in dev

**mcpServers**:
- `"next-devtools"`: `{ "type": "stdio", "command": "npx", "args": ["-y", "next-devtools-mcp@latest"] }` -- per Research R5

**skills**: Empty array for v1 (frontend-design skill from vercel-labs/agent-skills is aspirational -- include it if confident it works with project-scoped install, otherwise omit to avoid broken installs)

**claudeMd**:
- `file`: `"nextjs.md"`
- `content`: Write practical Next.js App Router guidance for Claude Code. Include:
  - Always use App Router (not Pages Router)
  - Server Components by default, `"use client"` only when needed
  - Use `next/image` for images, `next/link` for navigation
  - Use Route Handlers for API endpoints (`app/api/`)
  - Prefer Server Actions for mutations
  - Use `loading.tsx`, `error.tsx`, `not-found.tsx` for UI states
  - Collocate related files (component + styles + tests in same directory)
  </action>
  <verify>
`cat cli/presets/nextjs.json | python3 -m json.tool` -- valid JSON. Contains "next-devtools-mcp" in mcpServers. Has permissions.allow array with npm/npx entries. Has claudeMd.content with App Router guidance.
  </verify>
  <done>
`cli/presets/nextjs.json` is valid JSON with complete NextJS preset covering settings, MCP servers, and CLAUDE.md content.
  </done>
</task>
<task type="auto">
  <name>Create Convex preset definition</name>
  <files>
    cli/presets/convex.json
  </files>
  <action>
Create `cli/presets/convex.json` following the same preset schema. Include:

**name**: `"convex"`
**version**: `"1.0.0"`
**description**: `"Convex backend-as-a-service preset for Claude Code"`

**settings**:
- `permissions.allow`: `["Bash(npx convex *)", "Bash(npx convex dev *)"]` -- Convex CLI commands

**mcpServers**:
- `"convex"`: `{ "type": "stdio", "command": "npx", "args": ["convex", "mcp", "start"] }` -- per Research R5

**skills**: Empty array for v1 (no known Convex skill repo yet)

**claudeMd**:
- `file`: `"convex.md"`
- `content`: Write practical Convex development guidance for Claude Code. Include:
  - Convex functions live in `convex/` directory
  - Use `query`, `mutation`, `action` function types appropriately
  - Queries are reactive and cached -- use for reads
  - Mutations are transactional -- use for writes
  - Actions are for side effects (API calls, file I/O)
  - Schema defined in `convex/schema.ts` using `defineSchema` and `defineTable`
  - Use `v` validators for all function arguments
  - Index definitions go in schema for query performance
  - Use `npx convex dev` for local development with hot reload
  - Use `useQuery`, `useMutation`, `useAction` hooks in React components
  </action>
  <verify>
`cat cli/presets/convex.json | python3 -m json.tool` -- valid JSON. Contains "convex" in mcpServers with correct npx args. Has permissions.allow with convex entries. Has claudeMd.content with Convex guidance.
  </verify>
  <done>
`cli/presets/convex.json` is valid JSON with complete Convex preset covering settings, MCP servers, and CLAUDE.md content.
  </done>
</task>
</tasks>
<verification>
1. `python3 -m json.tool cli/presets/nextjs.json` -- valid JSON, no syntax errors
2. `python3 -m json.tool cli/presets/convex.json` -- valid JSON, no syntax errors
3. `grep "next-devtools-mcp" cli/presets/nextjs.json` -- confirms Next.js MCP server entry
4. `grep '"convex"' cli/presets/convex.json` -- confirms Convex MCP server entry
5. Both files have `name`, `version`, `description`, `settings`, `mcpServers`, `claudeMd` top-level keys
</verification>
<success_criteria>
- Both preset JSON files are valid and parseable
- NextJS preset includes next-devtools MCP server with correct npx args
- Convex preset includes convex MCP server with correct npx args
- Both presets include meaningful CLAUDE.md content with framework-specific guidance
- Both presets include appropriate permission allow entries
- Preset names match filename stems (nextjs, convex)
</success_criteria>
<output>
02-SUMMARY.md
</output>
